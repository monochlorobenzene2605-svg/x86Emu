class Emulator:
    None
class Environment:
    # レジスタは辞書で持ったほうがいい気がする
    self.registers =
if __name__ == '__main__':
    """
    # コマンドから実行バイナリのパスを取得
    while(Emu.env.eip!=0):
        (prefix,opcode,modrm,sib,displacement,imidiate) = fetch()
        Emulator.env = exec()
    print_env(Emulator.env)
    """
